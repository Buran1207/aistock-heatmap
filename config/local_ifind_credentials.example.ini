[ifind]
# 复制本文件为 config/local_ifind_credentials.ini 后填写；该文件已加入 .gitignore，不会上传 GitHub。
username = YOUR_IFIND_USERNAME
password = YOUR_IFIND_PASSWORD

[github]
# 可选：如果你以后想自动 git push，可以在本地配置 Git 或 token。
# token = YOUR_GITHUB_TOKEN
